<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<%= link_to 'Back to list', tasks_path %>
<%= link_to 'Update your task', edit_task_path %>
<%= link_to 'Delete', task_path(@task), method: :delete, data: {condirm: 'Are you sure you want to delete this task?'} %>
